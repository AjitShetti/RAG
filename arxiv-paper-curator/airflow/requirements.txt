# Airflow DAG-specific dependencies only
# DO NOT include sqlalchemy, pydantic, or any package that Airflow pins internally.
# These are installed via pip install --user inside the Airflow containers.

# arXiv API client (used by fetch_papers task)
arxiv>=2.1.0

# PDF extraction (used by parse_pdf task)
pdfminer.six>=20231228

# HTTP client (PDF download)
httpx>=0.27.0

# Retry logic
tenacity>=8.5.0

# DB driver (sync, for DAG tasks writing to postgres)
psycopg2-binary>=2.9.9

# OpenSearch client (for indexing tasks)
opensearch-py>=2.4.0

# Config reading
python-dotenv>=1.0.0
